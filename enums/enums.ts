// const AISLE = 0
// const MIDDLE = 1
// const WINDOW = 2

// if(seat ==0){

// }

enum SeatChoice {
    AISLE,
    MIDDLE,
    WINDOW
}
//default the enum variable gets 0 .. numbering 1st one gets 0 and other 1,2,3...
// however we can change it as well
const hcSeat = SeatChoice.AISLE